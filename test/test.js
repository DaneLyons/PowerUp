// Incomplete global test file. When it's done, you'll be able to run "mocha"
// and test everything.

//
// var fs = require('fs');
// 
// fs.readdir('.', function (err, files) {
//   if (err) { console.log(err); return process.exit(); }
//   
//   function requireDir(name, done) {
//     async.each(files, function (file, cb) {
//       fs.stat(file, function ())
//     }, function (err, files) {
//       return done(err, files);
//     })
//   }
//   async.each(files, function (file, cb) {
//     if (file.match(/\.js$/)) {
//       require(file);
//     } else {
//       requireDir(file);
//     }
//   }, function (err, files) {
//     
//   });
// });
